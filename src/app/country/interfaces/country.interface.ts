export interface CountryInterface {
    cca2: string;
    flag: string;      // Esto será para el ícono emoji (ej: 🇪🇸)
    flagSvg: string;   // Esto será para la imagen grande (URL)
    name: string;
    capital: string;
    population: number;
}
